import re

with open(r'd:/@VSwork/VS昭明计划VBA优化/_规则文档/全局_C昭明路线图.md', 'r', encoding='utf-8') as f:
    c = f.read()

# === CHAPTER 2: 二、操盘方法专题讨论 ===
# 专题一 → 2.1
c = c.replace('### 专题一：多长仓位与冲高策略的统一（2026-06-30）', '### 2.1 专题一：多长仓位与冲高策略的统一（2026-06-30）')

# Under 专题一:
# 0. 四区划总览 → #### 2.1.1
# 1. _多长策略 → #### 2.1.2
# 2. 冲高策略 → #### 2.1.3
# 3. 对VBA代码的影响 → #### 2.1.4
# 4. 待代码实现 → #### 2.1.5
c = c.replace('\n**0. 四区划总览（从好→差）**', '\n#### 2.1.1 四区划总览（从好→差）')
c = c.replace('\n**1. _多长策略 = 单一基仓**', '\n#### 2.1.2 _多长策略 = 单一基仓')
c = c.replace('\n**2. 冲高策略 = 浮仓（本质统一）**', '\n#### 2.1.3 冲高策略 = 浮仓（本质统一）')
c = c.replace('\n**3. 对VBA代码的影响**', '\n#### 2.1.4 对VBA代码的影响')
c = c.replace('\n**4. 待代码实现**', '\n#### 2.1.5 待代码实现')

# 专题二 → 2.2
c = c.replace('### 专题二：浮仓冲高策略的讨论（2026-06-30）', '### 2.2 专题二：浮仓冲高策略的讨论（2026-06-30）')
# Under 专题二: **问题：** → #### 2.2.1
c = c.replace('\n**问题：** 下周冲高与下日冲高都属于浮仓冲高策略', '\n#### 2.2.1 问题：下周冲高与下日冲高都属于浮仓冲高策略')
# But there might be other **问题：** in the file - make sure we only change this one
# The first one after the rename

# 专题三 → 2.3
c = c.replace('### 专题三：持仓组合设计（板块轮动 + 仓位控制）', '### 2.3 专题三：持仓组合设计（板块轮动 + 仓位控制）')
# Under 专题三: **问题：** → #### 2.3.1
c = c.replace('\n**问题：** 单一标的的持仓面临系统性风险', '\n#### 2.3.1 问题：单一标的的持仓面临系统性风险')
c = c.replace('\n**解决方案：**', '\n#### 2.3.2 解决方案：')
c = c.replace('\n**核心原则：**', '\n#### 2.3.3 核心原则：')

# === CHAPTER 3: 三、做成什么样 ===
# Fix numbering: 2.x → 3.x
c = c.replace('### 2.1 持仓规则验证体系', '### 3.1 持仓规则验证体系')
c = c.replace('#### 2.1.1 ', '#### 3.1.1 ')
c = c.replace('#### 2.1.2 ', '#### 3.1.2 ')

c = c.replace('### 2.2 每日盯盘无人操作推荐', '### 3.2 每日盯盘无人操作推荐')
c = c.replace('#### 2.2.1 ', '#### 3.2.1 ')
c = c.replace('#### 2.2.2 ', '#### 3.2.2 ')
c = c.replace('#### 2.2.3 ', '#### 3.2.3 ')
c = c.replace('#### 2.2.4 ', '#### 3.2.4 ')

c = c.replace('### 2.3 板块轮动分析与 Skill', '### 3.3 板块轮动分析与 Skill')
c = c.replace('#### 2.3.1 ', '#### 3.3.1 ')
c = c.replace('#### 2.3.2 ', '#### 3.3.2 ')
c = c.replace('#### 2.3.3 ', '#### 3.3.3 ')
c = c.replace('#### 2.3.4 ', '#### 3.3.4 ')
c = c.replace('#### 2.3.5 ', '#### 3.3.5 ')

# === CHAPTER 4: 四、什么时候做 ===
# Fix numbering: 3.x → 4.x
c = c.replace('### 3.1 实施阶段一：模型建立与验证（~13天）', '### 4.1 实施阶段一：模型建立与验证（~13天）')
c = c.replace('### 3.2 实施阶段二：系统自动化（~10天）', '### 4.2 实施阶段二：系统自动化（~10天）')
c = c.replace('### 3.3 实施阶段三：闭环与固化（~10天）', '### 4.3 实施阶段三：闭环与固化（~10天）')

# Under each 4.x, the M headings are fine (they're unique)

# === CHAPTER 5: 五、待完成零碎任务 ===
# Already correct (5.1, 5.2, 5.3)

with open(r'd:/@VSwork/VS昭明计划VBA优化/_规则文档/全局_C昭明路线图.md', 'w', encoding='utf-8') as f:
    f.write(c)
print('Done')