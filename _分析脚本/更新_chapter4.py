import re

with open(r'd:/@VSwork/VS昭明计划VBA优化/_规则文档/全局_C昭明路线图.md', 'r', encoding='utf-8') as f:
    c = f.read()

# Find the Chapter 4 boundaries
ch4_start = c.find('\n## 四、什么时候做：实施步骤\n')
ch5_start = c.find('\n## 五、待完成零碎任务\n')

if ch4_start < 0 or ch5_start < 0:
    print('ERROR: boundaries not found')
    exit()

# New Chapter 4 content
new_ch4 = '''## 四、什么时候做：实施步骤（共17天）

> **目的：** 将第二章已成型的方法论（四域、长基仓/周浮仓/日浮仓、组合设计）工程化为可运行的代码，打通从数据→判定→建议→执行的全链路。
> **核心理念：** 不从头研究，而是集成已有组件、填补缺失环节，每步都有可运行的产出物。

---

### 4.1 核心集成层（Core Integration，7天）

**目标：** 打通数据→四域判定→操作建议的完整链路

---

#### MC1 益盟四域桥接（1天）

**目的：** 将益盟公式输出的四域数据接入Python/Excel，使区划判定结果可被后续组件读取。

**输入：** 益盟公式输出的 `四域周`、`四域日`、`基本分类`、`信号分类` 列
**输出：** Python可读的区划数据文件（CSV/Excel）

**关键决策：** 直接读取益盟输出数据，不重新实现四域判定逻辑。

---

#### MC2 四域→操作映射表（1天）

**目的：** 建立从四域区划+策略条件到具体操作建议的确定性映射规则。

**输入：** 第二章的四域定义 + 长基仓/周浮仓/日浮仓策略条件
**输出：** 条件→操作的规则表

```
区划=_多长 AND WXZC>0 → 基仓持有
区划=_多长 AND 周信号触发 → 周浮仓可开
区划=_多长 AND 日信号触发 → 日浮仓可开
区划=_多被 → 可参与但仅小仓位
区划=_空看 → 不参与
区划=_空长 → 清仓退出
基本分类=买初 → 浮仓入场
基本分类=卖浮 → 浮仓退出
信号分类=警盈 → 止盈预警（仅供参考）
信号分类=警诱 → 诱多预警（仅供参考）
```

**里程碑：** 每只持仓都能自动映射出"持有/清仓/可加"的明确操作。

---

#### MC3 M4选股引擎重构（2天）

**目的：** 将旧M4选股引擎接入新的四域体系，候选标的附加上下层级区划标签。

**输入：** 旧M4选股引擎 + 四域数据
**输出：** 接入四域体系的选股引擎

**主要工作：**
1. 候选标的增加四域区划列（周域/日域）
2. 选股结果按区划排序（多长优先于多被）
3. 输出格式对齐第二章的小传结构

**里程碑：** 选股结果直接显示"区划+策略+操作"。

---

#### MC4 持仓小传生成器（2天）

**目的：** 每只持仓自动生成结构化小传，包含区划、策略、操作建议和原因。

**输入：** 区划+仓位分类+信号分类
**输出：** 每只持仓的操作小传

**小传结构：**
```
[标的] [区划] [策略]
  操作：[持有/清仓/加仓]
  原因：[WXZC状态/BTZ状态/信号分类等]
  预警：[警盈/警诱/无]
```

**里程碑：** 打开Excel，每只持仓直接看到操作建议，无需人为判断。

---

#### MC5 三周期统一（1天）

**目的：** 将日/周/月三个周期的区划判定合并为统一的持仓建议。

**输入：** 日域/周域/月域独立输出
**输出：** 统一的持仓建议

**规则：**
- 周域决定方向（多长→可操作，空长→退出）
- 日域决定执行点（买初/卖浮的具体时机）
- 月域决定大势（月多长时才可开新仓）

---

### 4.2 组合管理引擎（Portfolio Management，7天）

**目标：** 把2.3的组合设计（板块分散+仓位上限+ETF配比）变成可运行的代码

---

#### MP1 板块标签映射（1天）

**目的：** 每只股票关联所属板块，为组合分散提供基础数据。

**输入：** 股票代码列表
**输出：** 每只股票→所属板块（申万一级行业）

**关键决策：** 采用申万一级行业分类，不细化到二级，保证分类稳定。

---

#### MP2 组合仓位计算器（2天）

**目的：** 根据区划+板块+当前持仓，计算建议的持仓调整方案。

**输入：** 区划+板块+当前持仓数量
**输出：** 建议持仓调整（增减量）

**约束规则：**
```
单板块仓位 ≤ 30%（避免板块集中风险）
单标的仓位 ≤ 15%（避免个股风险）
多长基仓 3~5只 40~60%
周浮仓 2~3只 15~25%
日浮仓 1~2只 5~10%
宽基ETF 1~2只 15~25%
现金 5~10%
```

**算法：**
1. 遍历所有持仓，读取当日四域区划
2. 按板块聚合，计算各板块仓位占比
3. 若某板块>30%：超限部分标记为减仓候选
4. 若某标的区划非_多长：标记为退出候选
5. 对比当前仓位vs目标仓位 → 生成调整清单

---

#### MP3 ETF自动配比（1天）

**目的：** 当单板块超限时，超额部分自动转配对应板块ETF。

**输入：** 超限板块及超额量
**输出：** ETF配置建议

**规则：**
- 超额部分按比例分配到沪深300ETF/中证500ETF
- 不超限时不强制配置ETF
- ETF本身不计入板块仓位限制

---

#### MP4 板块轮动信号接入（2天）

**目的：** 将板块指数的四域判定作为调仓信号。

**输入：** 板块指数的四域区划
**输出：** 板块调仓信号

**规则：**
- 板块进_多长区域 → 该板块可适度增加配置
- 板块进_空长区域 → 减仓该板块，转配ETF
- 板块进_多被区域 → 维持现有仓位，不增不减

---

#### MP5 止损/再平衡触发器（1天）

**目的：** 区划变化时自动触发预警。

**输入：** 每日区划变化
**输出：** DJE跌破/区划切换预警

**规则：**
- 基仓退出条件：WXZC<0（等价于DJE跌破）
- 浮仓退出条件：下破DJC
- 单板块仓位超限：触发再平衡

---

### 4.3 决策输出与审计（Output & Audit，3天）

**目标：** 人只需阅读并执行，不需要自己做任何判断

---

#### MO1 每日操盘清单输出（1天）

**目的：** 整合MC+MP的所有输出，生成一份可直接执行的操盘清单。

**输入：** MC1-MC5 + MP1-MP5 的输出
**输出：** 一份可执行的操盘清单

**清单格式：**
```
【今日操盘清单】日期

── 已有持仓操作 ──
[标的] [板块] 区划=[区划] → [操作]（[原因]）

── 板块仓位监控 ──
[板块]：[仓位]%（上限30%）→ [状态]

── 候选推荐 ──
[标的]（板块=[板块]）区划=[区划] → 可开[策略]
```

---

#### MO2 交割单合规审计（1天）

**目的：** 对照四域规则审计历史交易，让违规操作显性化。

**输入：** 历史交割单
**输出：** 每笔交易的合规评价

**审计规则：**
1. 区划=_空长时是否执行了清仓 → 若未执行，标记违规
2. 区划≠_多长时是否开新仓 → 若开了，标记违规
3. DJE之下是否开仓 → 若开了，标记违规
4. 浮仓是否超过基仓 → 若超过，标记违规

---

#### MO3 流水线串联（1天）

**目的：** 将MC1-MC5 + MP1-MP5 + MO1-MO2 串联为一条一键运行的流水线。

**输入：** 各模块脚本
**输出：** `python run_daily.py` 一键运行入口

**流水线：**
```
数据更新 → 四域桥接 → 操作映射 → 组合计算 → 小传生成 → 操盘清单输出
```

**里程碑：** 每日一键运行，输出完整操盘清单，人只需阅读和执行。'''

c = c[:ch4_start] + new_ch4 + c[ch5_start:]

with open(r'd:/@VSwork/VS昭明计划VBA优化/_规则文档/全局_C昭明路线图.md', 'w', encoding='utf-8') as f:
    f.write(c)
print('Chapter 4 updated')