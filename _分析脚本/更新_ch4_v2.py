import re

with open(r'd:/@VSwork/VS昭明计划VBA优化/_规则文档/全局_C昭明路线图.md', 'r', encoding='utf-8') as f:
    c = f.read()

# Find the Chapter 4 boundaries
ch4_start = c.find('\n## 四、什么时候做：实施步骤（共17天）\n')
ch5_start = c.find('\n## 五、待完成零碎任务\n')

if ch4_start < 0 or ch5_start < 0:
    print('ERROR: boundaries not found')
    exit()

new_ch4 = r'''## 四、什么时候做：实施步骤（共17天）

> **目的：** 将第二章已成型的方法论（四域、长基仓/周浮仓/日浮仓、组合设计）工程化为可运行的代码系统。
> **核心理念：** 方法论独立于任何终端。VBA引擎是核心计算层，Python是集成与输出层，益盟仅为末梢展示通道，不作为中继依赖。
> **已有资产：** M1-M3 已完成产出物（策略映射文档、回测框架、策略验证结论）将作为本阶段的数据基础和验证基准。

---

### 4.1 核心引擎层（Engine Core，6天）

**目标：** 在VBA引擎中实现四域体系，确保独立于益盟运行，同时为Python层提供统一数据接口。

---

#### MC1 VBA四域判定引擎（2天）

**目的：** 在VBA引擎中实现四域判定逻辑（四域周、四域日、基本分类、信号分类），脱离对益盟公式的依赖。

**输入：** 已有的VBA数据引擎输出（BTZ/WXCD/柱排/冲高提示等）
**输出：** VBA内置的四域判定函数 + 数据列

**已有资产（M1已完成产出）：**
- `M1.1_策略_指标映射表.md` — 策略条件与VBA变量的对应关系
- `M1.2_柱排量化规则.md` — 柱排字符串解析规则
- `M1.3_三层策略伪代码.md` — 入场/出场/仓位的伪代码逻辑

**关键决策：** 四域判定逻辑直接在VBA中编写函数，不依赖益盟公式。益盟公式中的四域实现作为验证参考，不作为数据源。

---

#### MC2 VBA→Python 数据桥接（1天）

**目的：** VBA引擎的输出（算展文件/CSV）可被Python层稳定读取。

**输入：** VBA引擎生成的算展文件（`算展*.xlsx`）
**输出：** Python可读的结构化数据（CSV/DataFrame）

**已有资产：** `_回测框架/M2_1_数据加载.py` — 已有的数据读取模块

---

#### MC3 M4选股引擎重构 + 小传生成器（2天）

**目的：** 将旧M4选股引擎接入四域体系，并自动生成持仓小传。

**输入：** 四域数据 + 旧M4选股引擎
**输出：** 接入四域体系的选股引擎 + 小传生成器

**已有资产：**
- `M4.1_选股引擎.py` — 旧M4选股引擎代码
- `M4.3_每日选股.py` — 选股入口

**主要工作：**
1. 候选标的增加四域区划列（周域/日域）
2. 输出格式对齐第二章的小传结构
3. 每只持仓自动生成：区划 + 策略 + 操作建议 + 原因

---

#### MC5 四域伪代码文档化（1天）

**目的：** 将第二章的方法论编写为可读的技术文档，作为VBA/Python/益盟三方实现的统一参照。

**已有资产（M1.3 三层策略伪代码 → 扩展为四域伪代码）：**
- 原M1.3覆盖三层策略入场/出场逻辑
- 需要补充四域判定、仓位退出规则的伪代码

**输出：** `四域_实现指南.md` — 包含判定逻辑、迁移指南、测试用例

---

### 4.2 组合管理引擎（Portfolio Management，7天）

**目标：** 把2.3的组合设计（板块分散+仓位上限+ETF配比）变成可运行的代码，独立于任何终端。

---

#### MP1 板块标签映射（1天）

**目的：** 每只股票关联所属板块（申万一级行业），为组合分散提供基础数据。

**输出：** 板块-股票对照表（CSV/VBA模块）

---

#### MP2 组合仓位计算器（2天）

**目的：** 根据四域区划 + 板块 + 当前持仓，计算建议的持仓调整方案。

**约束规则：**
- 单板块 ≤ 30%，单标的 ≤ 15%
- 多长基仓 40~60%，周浮仓 15~25%，日浮仓 5~10%
- ETF 15~25%，现金 5~10%

**算法：**
1. 遍历所有持仓，读取当日四域区划
2. 按板块聚合，计算各板块仓位占比
3. 超限标记减仓，区划非多长标记退出
4. 对比当前仓位 vs 目标仓位 → 调整清单

---

#### MP3 ETF自动配比（1天）

**目的：** 当单板块超限时，超额部分自动转配对应板块ETF。

---

#### MP4 板块轮动信号接入（2天）

**目的：** 将板块指数的四域判定作为调仓信号。

**规则：**
- 板块进_多长 → 可增配
- 板块进_空长 → 减仓转ETF
- 板块进_多被 → 维持

---

#### MP5 止损/再平衡触发器（1天）

**规则：**
- 基仓退出：WXZC<0
- 浮仓退出：下破DJC
- 板块超限：触发再平衡

---

### 4.3 决策输出与前端（Output & Frontend，4天）

**目标：** 人只需阅读并执行，不需要自己做任何判断。输出形式不限于Excel，可扩展为网页。

---

#### MO1 每日操盘清单输出（1天）

**整合各模块输出，生成可执行的操盘清单。**

**清单格式：**
```
【今日操盘清单】日期
── 已有持仓 ──
[标的] [板块] 区划=[区划] → [操作]（[原因]）
── 板块仓位 ──
[板块]：[仓位]%（上限30%）→ [状态]
── 候选推荐 ──
[标的] 区划=[区划] → 可开[策略]
```

---

#### MO2 VBA引擎优化与自动化（1天）

**目的：** 减少VBA全量结算时间，提升数据更新效率。

**已有资产：** M0数据更新流程（Akshare下载 + VBA结算）
**主要工作：** 优化VBA结算流程，减少从数据下载到算展输出的全链路耗时。

---

#### MO3 交割单合规审计（1天）

**目的：** 对照四域规则审计历史交易。

**审计规则：**
1. 区划=_空长时是否执行清仓
2. DJE之下是否开仓
3. 浮仓是否超过基仓

---

#### MO4 流水线串联 + Python网页雏形（1天）

**目的：** 串联所有模块，构建一键运行入口，并探索Python网页作为未来前端。

**输出：** `run_daily.py` — 一键运行入口
**探索：** 简易HTML看板（替代纯Excel输出）

**已有资产：**
- M4.3 `M4_3_每日选股.py` — CLI入口，可作为流水线的一部分
- M2.1-M2.5 回测框架 — 验证流水线输出正确性
- M3 策略验证结论 — 作为流水线产出的预期基准

**架构图：**
```
VBA引擎（数据+判定） → Python（选股+组合+清单） → 输出（Excel/网页）
  ↑                            ↑
  ├─ 算展文件                  ├─ M4选股引擎
  ├─ 四域判定                  ├─ 组合管理
  └─ 基本分类/信号分类          └─ 操盘清单
```

---

> **注：** 益盟作为末梢应用，在VBA引擎完成后，可单独配置益盟公式读取相同数据源，但不作为本阶段目标。完成本阶段后，四域体系可独立运行于 VBA + Python + 网页 三端。'''

c = c[:ch4_start] + new_ch4 + c[ch5_start:]

with open(r'd:/@VSwork/VS昭明计划VBA优化/_规则文档/全局_C昭明路线图.md', 'w', encoding='utf-8') as f:
    f.write(c)
print('Chapter 4 updated')