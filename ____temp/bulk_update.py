import re

with open(r'_主文档/MC3.1_研究月基策略.md', 'r', encoding='utf-8') as f:
    content = f.read()

# ============================================================
# 1. Merge 1.1-1.3 into 1.1 with sub-headings
# ============================================================
old_ch1 = """### 1.1 核心规则

```
只做WXCD ∈ {金, 银}，排除屎尿唏嘘
金升（CD>0+ZC>0+ZB>0）才正式进场交易
银升（CD≤0+ZD>0+ZC>0+ZB>0）只跟踪，不算交易
```

### 1.2 数据支撑：银升 vs 金升 全量对比

| 指标 | 银升假信号（50.1%概率） | 金升真信号 |
|:----|:---------------------:|:----------:|
| **胜率** | **15.8%** | **61.9%** |
| **收益均值** | **-4.3%** | **19.9%** |
| **收益中位数** | **-3.8%** | **2.2%** |
| 总次数 | 30,890次 | 99,901次 |

**银升的命运：**
- 61,680次银升跟踪中，**50.1%（30,890次）是假信号**，直接退出不转金升
- 这些假信号**84.2%亏损**，均值-4.3%，中位数-3.8%
- 只有**49.7%（30,682次）成功转金升**，转金升后收益与直接金升相同

**金升的效果：**
- 61.9%胜率，均值19.9%，中位数2.2%

### 1.3 结论

**银升不能作为交易依据。** 50%概率是假信号，亏损率84.2%。必须等银升转金升（CD>0确认）才能进场。命分只统计金升阶段的收益，银升阶段完全不计入。

---

## 第二章：月基策略分类（周层四域→七分类）

> 月基策略的持仓状态按**周层四域**映射为7类，每类的5周内WXZB<0概率差异显著。

基于全量7,463只×422万周数据，验证结论如下：

| 类别 | 样本 | 5周续持率 | 5周转负率 | 占比 |
|:----|:---:|:---------:|:---------:|:----:|
| **多长(积极)** | 42万 | **71.8%** | 28.2% | 10.0% |
| **多长(不定)** | 58万 | **59.4%** | 40.6% | 13.8% |
| **多长(消极)** | 103万 | **43.1%** | **56.9%** | 24.6% |
| **多被(金)** | — | — | — | — |
| **多被(银)** | — | — | — | — |
| **NA(空看)** | — | — | — | — |
| **NA(空长)** | 216万 | **6.2%** | 93.8% | 51.7% |

> 注：多被/空看/空长的具体延续率待后续统计，多长三分类沿用原验证数据。

### 2.1 周层四域 ↔ 月基策略映射"""

new_ch1 = """### 1.1 祖训详解

#### 1.1.1 核心规则

```
只做WXCD ∈ {金, 银}，排除屎尿唏嘘
金升（CD>0+ZC>0+ZB>0）才正式进场交易
银升（CD≤0+ZD>0+ZC>0+ZB>0）只跟踪，不算交易
```

#### 1.1.2 数据支撑：银升 vs 金升 全量对比

| 指标 | 银升假信号（50.1%概率） | 金升真信号 |
|:----|:---------------------:|:----------:|
| **胜率** | **15.8%** | **61.9%** |
| **收益均值** | **-4.3%** | **19.9%** |
| **收益中位数** | **-3.8%** | **2.2%** |
| 总次数 | 30,890次 | 99,901次 |

**银升的命运：**
- 61,680次银升跟踪中，**50.1%（30,890次）是假信号**，直接退出不转金升
- 这些假信号**84.2%亏损**，均值-4.3%，中位数-3.8%
- 只有**49.7%（30,682次）成功转金升**，转金升后收益与直接金升相同

**金升的效果：**
- 61.9%胜率，均值19.9%，中位数2.2%

#### 1.1.3 结论

**银升不能作为交易依据。** 50%概率是假信号，亏损率84.2%。必须等银升转金升（CD>0确认）才能进场。命分只统计金升阶段的收益，银升阶段完全不计入。

---

## 第二章：月基策略分类（周层四域→八分类）

### 2.1 周层四域 → 月基八分类"""

content = content.replace(old_ch1, new_ch1)

# ============================================================
# 2. Replace old 2.1 content with 2.1.1 周层四域定义 + 2.2 月基八分类映射
# ============================================================
old_21 = """```
周层四域                 月基策略(7类)
┌───────────┐          ┌──────────────────┐
│  多长      │ ← ZC>0  │ 多长(积极)       │ 金+龙猪+升排
│ (三多全)   │  +CD>0  │ 多长(消极)       │ 有消极信号
│           │  +ZB>0  │ 多长(不定)       │ 其他
├───────────┤          ├──────────────────┤
│  多被      │ ← ZC>0  │ 多被(金)         │ 金系ZB≤0
│           │ 但CD≤0  │ 多被(银)         │ 银系/其他CD≤0
│           │ 或ZB≤0  │                  │
├───────────┤          ├──────────────────┤
│  空看      │ ← ZC≤0  │ NA(空看)         │ ZC≤0但CD/ZB有正
├───────────┤ 但CD/ZB  ├──────────────────┤
│  空长      │ 有正     │ NA(空长)         │ 三空全
│           │ ← 三空全 │                  │
└───────────┘          └──────────────────┘
```

### 2.2 分类判定流程

从周层四域出发，多长内部再分积极/消极/不定："""

new_21 = """#### 2.1.1 周层四域定义

基于 ZC（C线）、CD（大局）、ZB（稳线）三值判定，共 4 种域状态：

```
ZC>0? ──NO──→ CD≤0且ZB≤0? ──YES──→ 空长（三空全）
│                    NO  ──→ 空看（ZC≤0，但CD或ZB有正）
│
└──YES──→ CD>0且ZB>0? ──YES──→ 多长（三多全）
                    NO  ──→ 多被（ZC>0，但CD≤0或ZB≤0）
```

| 四域 | ZC | CD | ZB | 含义 |
|:----|:--:|:--:|:--:|:------|
| **多长** | >0 | >0 | >0 | 三多全，趋势完好 |
| **多被** | >0 | ≤0或≤0 | ≤0或≤0 | C线守住，但大局或稳线失守 |
| **空看** | ≤0 | >0或>0 | >0或>0 | C线失守，但CD或ZB有正，有反弹可能 |
| **空长** | ≤0 | ≤0 | ≤0 | 三空全，最弱状态 |

### 2.2 月基八分类映射

> 月基策略的持仓状态按**周层四域**映射为8类，每类的5周内WXZB<0概率差异显著。

基于全量7,463只×422万周数据，验证结论如下：

| 类别 | 样本 | 5周续持率 | 5周转负率 | 占比 |
|:----|:---:|:---------:|:---------:|:----:|
| **多长(积极)** | 51万 | **68.8%** | 31.2% | 12.1% |
| **多长(不定)** | 17万 | **61.3%** | 38.7% | 4.2% |
| **多长(消极)** | 53万 | **55.7%** | **44.3%** | 12.5% |
| **多被(金)** | 23万 | 40.4% | 59.6% | 5.5% |
| **多被(银)** | 58万 | 19.7% | 80.3% | 13.9% |
| **NA(空看)** | 25万 | 10.7% | 89.3% | 6.1% |
| **NA(空长)** | 193万 | 11.5% | 88.5% | 45.7% |

#### 2.2.1 各项定义（与VBA一致）

| 类别 | 判定条件 |
|:----|:---------|
| **多长(积极)** | 多长 + 波型含"龙猪" + 柱排以"升"开头 |
| **多长(消极)** | 多长中，非积极且有消极信号（按优先级）：①跌吞(本周阴吞前阳) ②ZA下降 ③本周涨<0 ④ZC≤0 ⑤震正波型 |
| **多长(不定)** | 多长中，非积极且无消极信号 |
| **多被(金)** | ZC>0 + (CD≤0或ZB≤0) + WXCD含"金" |
| **多被(银)** | ZC>0 + (CD≤0或ZB≤0) + WXCD含"银" |
| **多被(唏)** | ZC>0 + (CD≤0或ZB≤0) + WXCD不含金银（屎尿唏嘘） |
| **NA(空看)** | ZC≤0 + (CD>0或ZB>0) |
| **NA(空长)** | ZC≤0 + CD≤0 + ZB≤0 |

#### 2.2.2 月基状态细分流程

从周层四域出发，多长内部再分积极/消极/不定，多被按WXCD分金/银/唏："""

content = content.replace(old_21, new_21)

# ============================================================
# 3. Update 2.2 判定流程 content
# ============================================================
old_22 = """```
周层四域判定：
  ZC>0? → 否 → CD>0或ZB>0? → 是 → NA(空看)
  │                            否 → NA(空长)
  │
  └── ZC>0? → 是 → CD>0且ZB>0? → 是 → 多长
                    │                 ├── 金+龙猪+升排? → 多长(积极)
                    │                 ├── 消极信号?    → 多长(消极)
                    │                 └── 其他         → 多长(不定)
                    │
                    └── CD≤0或ZB≤0? → 多被
                                      ├── WXCD=金? → 多被(金)
                                      └── WXCD≠金? → 多被(银)"""

new_22 = """```
周层四域判定：
  ZC>0? → 否 → CD>0或ZB>0? → 是 → NA(空看)
  │                            否 → NA(空长)
  │
  └── ZC>0? → 是 → CD>0且ZB>0? → 是 → 多长
                    │                 ├── 龙猪+升排?     → 多长(积极)
                    │                 ├── 有消极信号?    → 多长(消极)
                    │                 └── 其他          → 多长(不定)
                    │
                    └── CD≤0或ZB≤0? → 多被
                                      ├── WXCD含"金"? → 多被(金)
                                      ├── WXCD含"银"? → 多被(银)
                                      └── 其他        → 多被(唏)
```

消极信号优先级（从强到弱）：①跌吞(阴吞前阳) ②ZA下降 ③本周涨<0 ④ZC≤0 ⑤震正波型

### 2.3 各类信号详解"""

content = content.replace(old_22, new_22)

# ============================================================
# 4. Add ZC≤0 row to 消极信号 table
# ============================================================
content = content.replace(
    "| \U0001f6a8 本周涨<0 | 63.1% | 周线下跌 | 双重确认时减仓 |\n\n**③ 多长(不定)**",
    "| \U0001f6a8 本周涨<0 | 63.1% | 周线下跌 | 双重确认时减仓 |\n| \U0001f6a8 ZC≤0 | — | C线失守 | 出现即准备减仓 |\n| \U0001f6a8 震正波型 | 54.5% | 趋势动荡不稳 | 龙猪转震正时警惕 |\n\n**③ 多长(不定)**"
)

# ============================================================
# 5. Update 多被 sections
# ============================================================
content = content.replace(
    "**④ 多被(金) —— 金系ZB≤0**\n- WXCD=金但ZB≤0，退出信号触发",
    "**④ 多被(金) —— WXCD含\"金\"**\n- WXCD=金但ZB≤0，退出信号触发"
)

content = content.replace(
    "**⑤ 多被(银) —— 银系/其他CD≤0**\n- ZC>0但CD不满足，需WXAB或WXZB或日类DXAB带动才能解套\n- 等待带动信号转多长\n\n**⑥ NA(空看) / NA(空长)**",
    "**⑤ 多被(银) —— WXCD含\"银\"**\n- ZC>0但CD不满足，需WXAB或WXZB或日类DXAB带动才能解套\n- 等待带动信号转多长\n\n**多被(唏) —— 其他（屎尿唏嘘）**\n- WXCD不含金银，趋势极弱\n- 月基不参与\n\n**⑥ NA(空看) / NA(空长)**"
)

# ============================================================
# 6. Update 操作规则: add 多被(唏)
# ============================================================
content = content.replace(
    "多被(银)   → 等待带动信号转多长，否则不参与\nNA(空看)   → 月基不参与，有反弹可能但非月基逻辑",
    "多被(银)   → 等待带动信号转多长，否则不参与\n多被(唏)   → 月基不参与，趋势极弱\nNA(空看)   → 月基不参与，有反弹可能但非月基逻辑"
)

# ============================================================
# 7. Renumber 2.4 → 2.5 for 操作规则
# ============================================================
content = content.replace("### 2.4 操作规则", "### 2.5 操作规则")

# ============================================================
# 8. Merge 1.2 content into 2.5 (操作规则)
# 操盘原则 as a blockquote before the code block
# ============================================================
操盘原则 = """

**进场条件：**
- 只做 WXCD ∈ {金, 银}，排除屎尿唏嘘
- **金升（CD>0+ZC>0+ZB>0）** 才正式进场
- 银升（CD≤0+ZD>0+ZC>0+ZB>0）只跟踪，等转金升再进场（50.1%假信号，不提前介入）
- 进场前检查 **月基命分（V1）**，命分<10分的股票天然不参与月基

**退出条件：**
- **硬退出：** WXZB从>0转≤0 → **立即卖出**（当周P(≥3%)=7.6%，几乎必亏）
- **软退出：** 消极区双重确认(ZA下降+本周涨<0) → 不等WXZB转负，提前减仓
- **最终退出：** WXZC<0 → 月基策略最终退出
- **多被(金)** → WXZB转负即卖，不等
- **多被(银)/多被(唏)/NA(空看)/NA(空长)** → 月基不参与，不持仓

"""

content = content.replace("### 2.5 操作规则\n\n```", "### 2.4 操作规则" + 操盘原则 + "```")

# Fix: 2.5 was already renamed above, but we need to handle the double rename
if "### 2.5 操作规则" in content:
    content = content.replace("### 2.5 操作规则", "### 2.4 操作规则")

with open(r'_主文档/MC3.1_研究月基策略.md', 'w', encoding='utf-8') as f:
    f.write(content)

print('OK')