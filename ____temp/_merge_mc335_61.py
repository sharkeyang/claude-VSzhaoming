# -*- coding: utf-8 -*-
"""合并 §6.1.1 指导思想 + §6.1.1.1 抓大看小（以§6.1.1为主体）
+ 删除 §6.1.2 三层漏斗操作规则（并入§6.1总览表）
"""
import io, re

OUT = '_主文档/MC3.3.5_甲乙己日等型交叉分析报告.md'

with io.open(OUT, encoding='utf-8') as f:
    c = f.read()

# ============ 1. 合并 §6.1.1 + §6.1.1.1 ============

# 定位 §6.1.1 到 §6.1.2 之间的内容（§6.1.1 + §6.1.1.1）
m611 = re.search(r'#### 6\.1\.1 指导思想：用户操作理念', c)
m612 = re.search(r'#### 6\.1\.2 三层漏斗操作规则', c)
assert m611 and m612, '定位 §6.1.1 失败'

# 合并后的 §6.1.1 内容（理念五层 + 决策顺序合并表 + DXZB退出规则）
merged_611 = '''#### 6.1.1 指导思想：用户操作理念（沃土→甲乙己不破DJB→升管→垒升→剔诱多）

> **课题（用户提出，2026-09-16）：** 在沃土，寻找 DXAB（甲乙己，不破DJB不出）+ 在升管 + 垒升，剔除诱多。**逐层评论确认：此理念 = "抓大看小"五层决策框架的完整落地，每一层都已有全量数据支撑，非新理念，而是对已验证结论的有机整合。**

**抓大看小 = 决策顺序 × 操作理念（同一思想，合并表）：**

| 层 | 决策顺序（先看什么） | 操作理念（做什么） | 数据 |
|:--|:--|:--|:--|
| **第1层 大势** | DXZE>0（EMA120之上） | 沃土的前提 | 过滤底部震荡 |
| **第2层 沃土** | DXCD=上/忐 | ① 沃土 | +4.98pp（23.6%→28.6%）|
| **第3层 主升区** | DXZC>0（DJC之上） | — | 主升区 |
| **第4层 护型** | DXAB 甲乙己 | ② 甲乙己 + ②' 不破DJB不出 | P≥3% 27-30%；四象限 69.96% |
| **第5层 微观** | 升管？垒升？诱多？ | ③ 升管 + ④ 垒升 + ⑤ 剔诱多 | 升管25.14%；垒升98.07%；剔诱多24.9% |

> **核心原则：** 要在沃土（DXCD上忐；DXZE>0）再考虑微观！**不要在恶土考虑微观！** 不要因小失大踏空——先确认大势/沃土/护型，再考虑升管/垒升/诱多等微观细节。

> **核心：②' 不破DJB不出 = DXZB 退出规则（已全量验证，非待验证）。**
> - **DXZB>0（不破DJB）= 甲乙己护型**（无论 DXZC 正负）；**DXZB<0（破DJB）= 丙丁戊护型**。DXZB 符号与 DXZC 符号 + 护型类别**完全共线**——只需看 DXZB 一个指标即可决定退出。
> - **四象限获利性（全样本约2000万行）：** DXZA>0+DXZB>0 → **69.96% 坚定持有**（DJA之上+不破DJB）；DXZA>0+DXZB<0 → 60.52% 可继续持有（防踏空）；DXZA<0+DXZB>0 → 55.90% 可持有观察；DXZA<0+DXZB<0 → **41.62% 退出**（破DJA+护型转坏）。
> - **"不破DJB不出"与"DXAB甲乙己"完全一致**（甲乙己=DXZB>0，丙丁戊=DXZB<0），不抵触。
> - **分层决策：** 护型（DXZB）决定"是否预警"，DJA位置（DXZA）决定"是否退出"——**先看大势（DXZE/DXCD/DXZC），再看护型（DXAB），最后看微观（升管/垒升/诱多）**。

**DXZA × DXZB 四象限获利性（全样本）：**

| 场景 | 样本 | 5天≥3% | 操作 |
|:--|--:|--:|:--|
| **DXZA>0 + DXZB>0** | 8,646,933 | **69.96%** | **坚定持有**（DJA 之上 + 不破 DJB）|
| **DXZA>0 + DXZB<0** | 1,464,859 | 60.52% | **可继续持有**（还在 DJA 之上，即使破 DJB，防止踏空）|
| **DXZA<0 + DXZB>0** | 1,401,587 | 55.90% | **可持有观察**（跌破 DJA 但护型未坏，甲乙己）|
| **DXZA<0 + DXZB<0** | 8,766,783 | 41.62% | **退出**（跌破 DJA + 护型转坏）|

> **⚠️ 关键澄清（DXZA>0 + DXZB<0 是戊）：** DXZA>0 + DXZB<0（60.52%）**全部是戊（DXZA>0）护型**（样本 1,464,859）。**原计划也是不参与**（戊=方向不明过渡态，等待变为己或甲再参与）。所以"可继续持有"仅指**已持仓时**不急着退出（防止踏空），**空仓时不介入**（等转己/甲）。

**综合退出规则（与已有规则不抵触）：**

> **DXZB 退出规则（DXZB<0 才退出）与"DXAB 甲乙己"完全一致**（甲乙己=DXZB>0，丙丁戊=DXZB<0），不抵触。
>
> **与"DJA 之上跌连后不退出，等待下破 DJA 后选择好方向后再退出，防止踏空"也不抵触**——因为：
> - **护型转坏（DXZB<0）是预警**，但**只要还在 DJA 之上（DXZA>0）就不急着退出**（60.52% 仍可持有）
> - **跌破 DJA（DXZA<0）+ 护型转坏（DXZB<0）才是明确退出**（41.62%）
>
> **分层决策：** 第4层（护型 DXZB）决定"是否预警"，第5层（DJA 位置 DXZA）决定"是否退出"——**先看大势（DXZE/DXCD/DXZC），再看护型（DXAB），最后看微观（升管/垒升/诱多）**。

> **⚠️ 唯一需注意的取舍：** 沃土（35.6%）→ 甲乙己 → 升管 → 垒升 → 剔诱多，可操作范围收敛到极小，**交易频率低（§6.12.5 每年仅 10-14 次/股），但每次胜率高**。这是"少而精"的代价，需接受。

> **一句话：** 此理念 = "抓大看小"五层决策框架的完整落地——**先大势（DXZE>0）→ 沃土（DXCD上/忐）→ 主升区（DXZC>0）→ 护型（甲乙己，不破DJB不出）→ 微观（升管/垒升/剔诱多）**，每层都已有全量数据支撑，非新理念。

'''

# 替换 §6.1.1 到 §6.1.2 之间的内容为合并后的 §6.1.1
c = c[:m611.start()] + merged_611 + c[m612.start():]

# ============ 2. 删除 §6.1.2 三层漏斗操作规则（并入§6.1总览表） ============

# 定位 §6.1.2 到 §6.1.3 之间的内容
m612 = re.search(r'#### 6\.1\.2 三层漏斗操作规则', c)
m613 = re.search(r'#### 6\.1\.3 操作逻辑明确化', c)
assert m612 and m613, '定位 §6.1.2 失败'

# §6.1.2 的内容（代码块）并入 §6.1 总览表下方，作为"操作细则"
# 提取 §6.1.2 的代码块内容
block_612 = c[m612.start():m613.start()]
# 提取代码块（```...```）
code_match = re.search(r'```\n(.*?)```', block_612, re.DOTALL)
assert code_match, '提取代码块失败'
code_content = code_match.group(1).strip()

# 在 §6.1 总览表统一说明之后插入"操作细则"
# 定位 §6.1 总览表的统一说明（⚠️ 三层漏斗）
m_unify = re.search(r'> \*\*⚠️ 三层漏斗（2026-09-18 统一）：\*\*.*?\*\*。\n', c, re.DOTALL)
assert m_unify, '定位统一说明失败'

op_detail = f'''
**操作细则（三层漏斗把可操作范围收敛到极小）：**

```
{code_content}
```
'''

# 在统一说明之后插入操作细则
c = c[:m_unify.end()] + op_detail + c[m_unify.end():]

# 删除 §6.1.2 小节（现在代码块已并入，删除整个 §6.1.2 块）
m612 = re.search(r'#### 6\.1\.2 三层漏斗操作规则', c)
m613 = re.search(r'#### 6\.1\.3 操作逻辑明确化', c)
c = c[:m612.start()] + c[m613.start():]

# ============ 3. 更新交叉引用 ============
# §6.1.1.1 → §6.1.1（抓大看小并入指导思想）
c = c.replace('§6.1.1.1', '§6.1.1')

# ============ 写入 ============
with io.open(OUT, 'w', encoding='utf-8') as f:
    f.write(c)

print('合并完成')
print('总行数:', c.count(chr(10))+1)
print('§6.1.1.1 残留:', c.count('6.1.1.1'))
print('§6.1.2 残留:', c.count('6.1.2 三层漏斗'))